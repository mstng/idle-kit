# idle-kit — 作業の引き継ぎ

このファイルは Claude Code が自動で読み込みます。
**設計の理由と各機能の解説は [README.md](./README.md) にあります。着手前に必ず読んでください。**
ここには README に書いていない「進め方」と「すでに踏んだ罠」だけを書きます。

---

## このプロジェクトの目的

放置育成ゲーム「そだつ」。ただし**遊べる完成品を作ることが目的ではありません**。

> 次にちゃんとした放置ゲーを作るときに、そのまま流用できる「型」を残す

判断に迷ったら、この一文に立ち返ってください。
「動けばいい実装」より「なぜそう作るのかが読み取れる実装」を選びます。

---

## 絶対に崩してはいけない設計原則

この3つが崩れると、このプロジェクトは存在意義を失います。

### 1. ゲームのルールは `src/game.js` の純粋関数だけに置く

DOM・localStorage・`Date.now()` のいずれも `game.js` の中で触らないでください。
現在時刻は必ず引数で受け取ります（`advanceTo(state, now)`）。

これを守っているおかげで、時刻を固定したテストが書け、`tools/simulate.js` で
数日ぶんのプレイを早送りできています。1か所でも `Date.now()` を埋め込むと両方が壊れます。

### 2. 時間を進める入口は `advanceTo` ひとつだけ

オンライン進行・オフライン進行・自動購入・はっぱの出現・実績判定を、
すべてこの関数の中で順に処理しています。

「オフラインのときだけ別処理」を書きたくなったら、それは間違いです。
別経路にすると必ず計算がズレます（このジャンルで最頻出のバグ）。

### 3. 判定に使う値は「減らないもの」だけ

進化・アンロック・実績・転生報酬の判定は、すべて累計値（`totalEarned` /
`lifetimeEarned` / `megumiEarned`）で行います。所持量で判定すると、
買い物した瞬間に進んだはずのものが巻き戻ります。

同じ理由で「所持」と「累計」は必ず別々に持ちます（`light`/`totalEarned`、
`megumi`/`megumiEarned`）。

---

## 作業の進め方

- **日本語で応答する。コードのコメントもすべて日本語。**
- **テストを必ずセットで書く**（`node:test`、依存パッケージなし）。
  ロジックを足したらテストも足す。現在125件。
- **ブラウザで実際に動かして確認してから完了と報告する。**
  「テストが通ったので大丈夫」は不可。実際、画面を見て初めて見つかった
  バグが複数あります（後述）。
- 数値をいじったら `npm run simulate` で影響を測る。体感で判断しない。
- コミットメッセージには「何を変えたか」ではなく**「なぜそう決めたか」**を書く。
  既存のコミットログが手本です。

```bash
npm test          # テスト
npm run serve     # http://localhost:8765
npm run simulate  # 1週間ぶんを早送りしてバランスを見る
npm run simulate -- --hours=24 --growth=0.2 --no-leaves
```

---

## リポジトリとプッシュ手順（重要）

**同じコードが2か所にあります。取り違えると作業が消えます。**

| 場所 | 役割 |
|---|---|
| `github.com/mstng/idle-kit` (private) | 本流。ここが正 |
| `~/vscode/idle-kit`（`vscode` モノレポの `feat/idle-kit` ブランチ内） | 開発の作業場所。`vscode` 側にリモートは無い |

### `~/vscode` で作業している場合

`idle-kit/` の履歴だけを切り出して push します。**通常の `git push` は使えません**
（`vscode` リポジトリにリモートが設定されていないため）。

```bash
cd ~/vscode
git add idle-kit && git commit   # コミットはモノレポ側に積む
git subtree split --prefix=idle-kit -b idle-kit-export
git push https://github.com/mstng/idle-kit.git idle-kit-export:main
git branch -D idle-kit-export
```

パス構造が違うのでコミットハッシュは両者で一致しません。中身は同一です。

### `mstng/idle-kit` を単独でクローンして作業する場合

ふつうに `git push` してください。こちらのほうが素直なので、
今後の作業はこの形に移行することを検討してもよいです。

### 未追跡のまま残しているもの

`~/vscode/.claude/launch.json`（プレビュー起動の設定）は意図的にコミットしていません。
`npm run serve` があるので無くても困りません。

---

## すでに踏んだ罠（同じ穴に落ちないために）

すべて実際に時間を取られたものです。

| 罠 | 症状 | 対処 |
|---|---|---|
| **ES モジュールのキャッシュ** | 直したのにブラウザの表示が変わらない。import 先まで個別にキャッシュされ、リロードでは剥がれない | `tools/serve.js` が `Cache-Control: no-store` を返す。それでも古い場合は `fetch(url, {cache:'reload'})` でモジュールを取り直してからリロード |
| **`[hidden]` が効かない** | 隠したはずの要素が表示される | `display: flex` のほうが強い。`[hidden] { display: none !important }` を CSS に入れてある |
| **巨大数を `Number` に戻すと誤差が復活** | コストが 192 ではなく 193 と表示された | `floor`/`ceil` は信用できる15桁で丸めてから整数化する（`bignum.js`） |
| **`pow` の対数経由の誤差** | `5^2` が `25.000000000000004` になり、切り上げが1ずれる | `Number` で正確に表せる範囲は直接計算する分岐を入れてある |
| **固定値が取り残される** | 終盤に育成段階が20秒で全部飛んだ | 倍率が無限に伸びる設計では固定値は必ず置いていかれる。`stageScale` で目標側も成長させた |

**最後の行が、このプロジェクトで一番大きい学びです。**
新しく固定の数値を置くときは「これは成長する側か、取り残される側か」を必ず確認してください。

---

## セーブデータを壊さないために

放置ゲーはセーブが壊れた瞬間に終わります。

- 状態の形を変えたら `SCHEMA_VERSION` を上げ、`MIGRATIONS` に**1段ぶんの**移行を足す
  （現在 v6。v1→v6 まで1段ずつ引き上げられることをテストで担保しています）
- 保存キー `idle-kit:save` は**絶対に変えない**。形式の変更は `version` で吸収する
- `deserialize` は壊れたデータ・未来のバージョン・項目の欠損のどれが来ても例外を投げない

---

## 現在地

実装済みの「型」は12個（詳細は README）。テスト125件。

段階のしきい値・転生・巨大数・自動購入・実績/アンロック・ランダムイベント・
一括購入・シミュレータ・PWA まで入っており、**型としては一周そろっています。**

### 次にやるとよいこと（優先度順）

1. **2周目以降の解禁要素** — 転生回数に応じて新しいアップグレードや段階を開放する。
   下記の「終盤に1周が長くなる」問題への手当てにもなる
2. **この型でテーマだけ変えた2本目を作る** — 本当に流用できる型かは、それでしか検証できない。
   流用できなければ、どこが密結合しているかが分かって、それ自体が収穫になる
3. **セーブのエクスポート／インポート** — 実際に公開するなら必須（localStorage は消える）

### 分かっているが直していない問題

終盤ほど1周が長くなります（周の中央値52分に対し、直近の周は1日13時間）。
転生の基準を変えても傾向は変わらず、**転生報酬を平方根にしている設計そのものの帰結**です。
バランス定数をいじっても解決しないので、解禁要素などゲーム内容側で手当てする領域になります。
詳細は README の「分かっている特性」を参照してください。
